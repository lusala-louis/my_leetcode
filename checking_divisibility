class Solution(object):
    def is_divisible_by_four(self, n):
        """
        :type n: int
        :rtype: bool
        """
        while n > 0:
            if n == 1 or n % 4 == 0:
                return True

            elif n % 4 != 0:
                return False
